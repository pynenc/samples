[project]
name = "pynenc-sample-search-algorithms-demo"
version = "0.1.0"
description = "Pynenc sample: classic search algorithms with every step distributed as a task"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pynenc[monitor]==0.3.1",
]

[dependency-groups]
dev = []
