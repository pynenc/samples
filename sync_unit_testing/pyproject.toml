[project]
name = "pynenc-sample-sync-unit-testing"
version = "0.1.0"
description = "Pynenc sample: unit testing with synchronous dev mode"
requires-python = ">=3.12"
dependencies = [
    "pynenc==0.3.1",
]

[dependency-groups]
dev = [
    "pytest",
]
